[pytest]
# format_tool_response and the MCP handlers are async; auto mode runs async
# test functions without per-test @pytest.mark.asyncio decorators.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
